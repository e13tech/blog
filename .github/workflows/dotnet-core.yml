name: .NET Core

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - shell: pwsh
      env:
        SUPER_SECRET: ${{ secrets.BLOG_PUSH_ACTION }}
      run: echo "secret- $env:API_TOKEN_GITHUB"
    - shell: pwsh
      run: echo "secret- ${{ secrets.BLOG_PUSH_ACTION }}"
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.100
    - name: Install dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --configuration Release --no-restore
    - name: pull blog-static output
      run: git clone https://${{ secrets.BLOG_PUSH_ACTION }}@github.com/e13tech/blog-static.git ../output
    - name: Run
      run: dotnet run --output ../output --noclean
    - run: git branch stage
      working-directory: ../output
    - run: git checkout stage
      working-directory: ../output
    - run: git add .
      working-directory: ../output
    - run: git config --global user.email "jj.bussert@gmail.com"
      working-directory: ../output
    - run: git config --global user.name "Automation"
      working-directory: ../output
    - run: git commit -m stage
      working-directory: ../output
    - run: git remote -v
      working-directory: ../output
    - name: git push
      run: git push --set-upstream blog-static stage
      working-directory: ../output
